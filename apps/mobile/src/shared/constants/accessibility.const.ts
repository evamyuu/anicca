// ─────────────────────────────────────────────────────────────────────────────
// Accessibility Constants
// WCAG 2.1 AA minimum requirements — per ANICCA_ROOT_STRUCTURE.md Part 8
// ─────────────────────────────────────────────────────────────────────────────

/** Minimum font size for regular text (WCAG 1.4.4) */
export const MIN_FONT_SIZE = 16; // px

/** Minimum font size in accessible mode */
export const ACCESSIBLE_FONT_SIZE = 18; // px

/** Minimum touch target size — Android (dp) */
export const MIN_TOUCH_TARGET_ANDROID = 48; // dp

/** Minimum touch target size — iOS (pt) */
export const MIN_TOUCH_TARGET_IOS = 44; // pt

/** Cross-platform minimum touch target */
export const MIN_TOUCH_TARGET = 48;

/** Minimum contrast ratio for normal text (WCAG 1.4.3) */
export const MIN_CONTRAST_RATIO = 4.5;

/** Minimum contrast ratio for large text (≥18pt or ≥14pt bold) */
export const MIN_CONTRAST_RATIO_LARGE = 3.0;
