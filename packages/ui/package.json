{
  "name": "@anicca/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Shared UI design system for Anicca (tokens + base components)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anicca/types": "workspace:*"
  },
  "devDependencies": {
    "@anicca/config": "workspace:*",
    "typescript": "^5.5.3"
  }
}
